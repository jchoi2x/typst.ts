# @jchoi2x packages: publish and install from GitHub Packages (not registry.npmjs.org).
# Publishing: set NODE_AUTH_TOKEN (e.g. GitHub Actions uses secrets.GITHUB_TOKEN).
# Installing: add a Personal Access Token with read:packages — see docs/github-packages.md
@jchoi2x:registry=https://npm.pkg.github.com
